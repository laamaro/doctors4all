
<div class="container">
  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1624727828489-a1e03b79bba8);">
  <div class="container">
    <h1>Agende agora sua consulta</h1>
    <p>Mais de 800 especialistas para te ajudar</p>
      <div class="container">
    <div class="row">

      <div class="col">
        <%= form_tag root_path, method: :get do %>
          <div class="col mt-3 mb-2">
            <%= text_field_tag :query,
                                params[:query],
                                class: "form-control",
                                placeholder: "Especialidade"%>
          </div>
          <div class="col mt-3 mb-2">
            <%= submit_tag "Pesquisar", class: "btn btn-success mt-2" %>
          </div>
        <% end %>
      </div>
      <div class="col mt-3 mb-2">
        <select class="form-control col-6" placeholder="localidade">
          <option>Localidade</option>
          <option>São Paulo</option>
          <option>Rio de Janeiro</option>
          <option>Curitiba</option>
        </select>
      </div>
    </div>
  </div>
  </div>
</div>

  <% @treatments.each do |treatment| %>
    <%= link_to treatment.specialty, treatment_path(treatment) %>
    <%= treatment.address %>
    <%= treatment.price %>

  <% end %>
</div>
